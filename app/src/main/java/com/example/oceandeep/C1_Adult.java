package com.example.oceandeep;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class C1_Adult extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_c1__adult);

        /*
        // Get the Intent that started this activity and extract the string
        Intent intent = getIntent();
        int randomID = Integer.parseInt(intent.getStringExtra(MainActivity.EXTRA_MESSAGE));

        TextView resultHere = (TextView) findViewById(R.id.resultHere);
        resultHere.setText("" + randomID);
        */
    }
}
